[1] 実行方法
(1)
$ ./build.sh
を実行すると、ソースコードがコンパイルされ"main"という実行プログラムが生成される。

(2)
$ ./main <ファイル名>
と実行すると、<ファイル名>のソースコードが実行される。
なおサンプルプログラムとして、"1.scm", ..., "5.scm"を用意している。

[2] format構文に関する補足
第1引数は"#t"、第2引数は文字列である必要があり、第3引数以降の引数の数は、第2引数に含まれる変換指示子の個数と同じである必要がある。
第2引数中の変換指示子は、数値の"~D"、文字列の"~A"のみに対応している。"~%"で改行を表す。